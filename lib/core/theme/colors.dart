import 'package:flutter/material.dart';

class TailOColors {
  // Primary Brand
  static const coral = Color(0xFFEE6C4D);
  static const primary = coral; // Alias for semantic naming

  // Semantic Colors (Success, Warning, Error)
  static const success = Color(0xFF34C759);
  static const warning = Color(0xFFFF9F0A);
  static const error = Color(0xFFFF453A);
  static const info = Color(0xFF0A84FF);

  // Backgrounds
  static const darkBg = Color(0xFF151515);
  static const lightBg = Color(0xFFFBEFE6);

  // Surface / Cards
  static const darkCard = Color(0xFF1C1C1E);
  static const lightCard = Colors.white;

  // Text / Content
  static const darkText = Color(0xFFFBEFE6);
  static const lightText = Color(0xFF151515);

  // Neutrals
  static const muted = Color(0xFF7D7D81);
  static const borderDark = Color(0x1FFFFFFF); // 12% White
  static const borderLight = Color(0x14000000); // 8% Black
}
